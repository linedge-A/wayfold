{
  "name": "wayfold-functions",
  "description": "Keyed Gemini proxy as a Firebase Functions Gen2 HTTPS function (backend-core + Wayfold domain pack).",
  "type": "commonjs",
  "main": "lib/index.js",
  "engines": { "node": "22" },
  "scripts": {
    "//": "Bundle index.ts + its ../ imports (server-core/domain/security, express, @google/genai, ingestion) into one self-contained file; firebase runtime provides firebase-functions.",
    "build": "esbuild index.ts --bundle --platform=node --format=cjs --target=node22 --external:firebase-functions --external:firebase-admin --outfile=lib/index.js",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-functions": "^6.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
